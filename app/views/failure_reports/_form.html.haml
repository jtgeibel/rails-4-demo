= simple_form_for(@failure_report, html: {class: 'form-horizontal' }) do |f|
  = f.error_notification
  = f.input :unit, hint: "The unit's serail number"
  = f.input :subject, hint: "Must be at least 15 characters"
  = f.input :description, :input_html => { :rows => 10 }
  = f.input :rma, label: "RMA number"
  = f.input :failcode, hint: "Must be an integer"
  .controls
    = f.button :submit