= link_to "Home", root_path, :class => 'brand'
%ul.nav
  %li= link_to "Failure reports", failure_reports_path
  %li= link_to "Units", units_path
%ul.nav.pull-right
  - if user_signed_in?
    %li.pull-right= link_to('Logout', destroy_user_session_path, :method=>'delete')
    %li.pull-right= link_to('Edit account', edit_user_registration_path)
  - else
    %li.pull-right= link_to('Login', new_user_session_path)
    %li.pull-right= link_to('Sign up', new_user_registration_path)
